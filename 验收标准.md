# 验收标准.md — Agentic EDA 系统验收项汇总(doc v1.2)

> 本文件把组件 A(skill_diagnose)/ B(skill_self_heal)/ C(CPlanner)/ 系统集成的验收项汇总成**可勾选表**,并写清验收流程(Claude 如何验收)。
>
> 所有测试命令在 `D:/eda agent system`(项目根 `eda-agent-system/`)下执行,WSL2 工具就绪。needs_eda marker 需 yosys/iverilog/opensta 装好;评审无工具时见 §6 替代证据。
>
> 量化门槛对齐 CONTRACTS.md v1.2 与三份组件文档。冲突时以契约为准。

---

## 1. 系统级硬指标(完赛奖四条,必过)

| # | 硬指标 | 量化门槛 | 测试方法 | 权重 | 勾选 |
|---|---|---|---|---|---|
| S1 | (1) 真实可跑组件 | Yosys/iverilog/OpenSTA 三 Tool 真跑通;A 真调 LLM 产结构化归因;B 真迭代 >=1 轮 | `pytest tests/test_yosys_tool.py tests/test_iverilog_tool.py tests/test_opensta_tool.py tests/test_diagnose_skill.py tests/test_self_heal_skill.py -v -m needs_eda` | 必过 | ☐ |
| S2 | (2) 清晰可调用接口 | eda self-heal / diagnose / report 三子命令可调;退出码 0/1/2/64 | `pip install -e . && eda --help` 列三子命令;§3 C5 交互断言 | 必过 | ☐ |
| S3 | (3) 实验证据/指标对比 | runs/ 落轨迹 + experiment_manifest.json(14 字段)+ experiment_summary.json;baseline vs self_heal 对比 | §3 集成 I3 + `scripts/summarize_eval.py` 产出 summary | 必过 | ☐ |
| S4 | (4) EDA 三赛道且 agentic | C 默认 LLM planner(ReAct);B 内部迭代+patch 回退;C 独立验证步 | §3 集成 I4 + trajectory 含 convergence_cause/best_iter + 独立验证步存在 | 必过 | ☐ |

---

## 2. 组件 A(诊断器)验收项

| # | 指标 | 量化门槛 | 测试方法 | 权重 | 勾选 |
|---|---|---|---|---|---|
| A1 | Top-1 根因命中率(加权) | 加权总分 >= 0.60(0.4*code + 0.4*root_cause + 0.2*hint) | `python scripts/eval_diagnose.py --corpus data/logs_corpus/corpus.jsonl` | 必过 | ☐ |
| A2 | 规则层覆盖率 | >= 40%(分母=corpus 全样本) | 同 A1 | 必过 | ☐ |
| A3 | confidence(LLM 层样本均值) | >= 0.60 | 同 A1 | 必过 | ☐ |
| A4 | confidence(规则层样本均值) | >= 0.70 | 同 A1 | 必过 | ☐ |
| A5 | 接口契约单测 | 100% 全绿 | `pytest tests/test_diagnose_skill.py -v`(无 needs_eda) | 必过 | ☐ |
| A6 | ErrorKB 增长用例 | >= 1 条(propose_pending → confirm → kb.patterns +1) | 手动跑 `scripts/confirm_kb_pattern.py` | 必过 | ☐ |
| A7 | LLM 调用次数(单次诊断) | <= 1 | trajectory 中 step=="llm" 最多 1 次 | 必过 | ☐ |
| A8 | 单次诊断墙钟 | p50 <= 20s 且 p95 <= 60s(含 LLM 等待) | eval 脚本记录 wall_time;R4 回退样本计 p50 不计 p95 | 必过 | ☐ |
| A9 | needs_rtl_patch 按 severity 判 | eda.rtl_syntax+severity=error → True;eda.budget_exhausted+warn → False | `pytest tests/test_diagnose_skill.py::test_needs_patch_severity` | 必过 | ☐ |
| A10 | confidence 饱和项 | evidence 长度 >=5 时 confidence 不再单调增 | `pytest tests/test_diagnose_skill.py::test_confidence_saturation` | 必过 | ☐ |
| A11 | 语料规模 | >= 30 条,synth/sim/sta 各 >=5,hard >=3 | `wc -l data/logs_corpus/corpus.jsonl` + 分桶统计 | 必过 | ☐ |
| A12 | 抽检一致性 | >= 0.8(偏 AI 成员抽检 20%) | 抽检报告 | 必过 | ☐ |
| A13 | as_tool 恒定语义 | _patch_source=="none"、_convergence_cause=="none"、_best_iter==-1 | `pytest tests/test_contracts.py::test_diagnose_as_tool_constants` | 必过 | ☐ |
| A14 | parsed._schema.contract_version | == CONTRACT_VERSION 常量(非裸字符串) | grep 源码无裸 "0.1.0" 在 to_parsed 路径 | 必过 | ☐ |

---

## 3. 组件 B(自修复闭环)验收项

| # | 指标 | 量化门槛 | 测试方法 | 权重 | 勾选 |
|---|---|---|---|---|---|
| B1 | 自修复通过率(单一硬门槛) | min(by_fault_type.passed/total) >= 0.50 且 bitwidth 类 >= 1 all_pass | `pytest tests/test_self_heal_skill.py::test_pass_rate -q -m needs_eda` + `scripts/summarize_eval.py` | 必过 | ☐ |
| B2 | 平均迭代次数 | mean(iterations | converged) <= 3.5 | 从 B1 成功 run 集合取均值 | 必过 | ☐ |
| B3 | 回滚机制单测 | 100% 通过(3 种坏 patch:语法错/应用失败/退化) | `pytest tests/test_self_heal_skill.py::test_rollback_unit -q` | 必过 | ☐ |
| B4 | trace 完整性 | 100%(iter_* 快照数==iterations;best/meta.json 含 4 字段;失败 run 允许 best_iter==-1) | `pytest tests/test_self_heal_skill.py::test_trace_integrity -q -m needs_eda` | 必过 | ☐ |
| B5 | 失败案例真实有结构 | >= 1 个 run convergence ∈ {budget,regression,max_iter} 且 trajectory >= 2 轮 | 跑 tiny_fsm/rtl_state_bug.v | 必过 | ☐ |
| B6 | 接口对齐 + 语义验收 | 100%(as_tool 字段对齐 + mock A fix_suggestion 进 B prompt) | `pytest tests/test_contracts.py::test_self_heal_as_tool_schema tests/test_self_heal_skill.py::test_consume_diagnose_fix_suggestion -q` | 必过 | ☐ |
| B7 | best_iter tie-break | 取最早达到 best_score 的轮;meta.json.candidates_at_best_score 与 trajectory 一致 | `pytest tests/test_self_heal_skill.py::test_best_iter_tiebreak` | 必过 | ☐ |
| B8 | fail_signals 包装 | root_causes 空时,fail_signals 包装成 ErrorItem(code=sim.fail_signal) 再喂 LLM | `pytest tests/test_self_heal_skill.py::test_fail_signals_wrapping` | 必过 | ☐ |
| B9 | STA 触发(时序闭环) | adder_pipe timing bug 至少 1 个 run 触发 STA 分支 | 跑 data/examples/adder_pipe/rtl_timing_bug.v,断言 trajectory 含 opensta_timing 步 | 必过 | ☐ |
| B10 | patch_source 失败 run | 失败 run 也记录"最后一次尝试"的 source(仅 iterations=0 且 budget 时允许 none) | `pytest tests/test_self_heal_skill.py::test_patch_source_on_failure` | 必过 | ☐ |

---

## 4. 组件 C(Planner)验收项

| # | 指标 | 量化门槛 | 测试方法 | 权重 | 勾选 |
|---|---|---|---|---|---|
| C1 | 不重新定义契约类型 | planner/ 与 cli.py 无 class ToolResult/RunReport/SkillResult 等同名定义 | `python scripts/check_no_subprocess.py`(ast.walk) | 必过 | ☐ |
| C2 | 不直接 subprocess | planner/ AST 无 subprocess.run/Popen 调用 | 同 C1 | 必过 | ☐ |
| C3 | 状态机能跑完 | 任意 RunRequest 不抛异常,status ∈ {ok,failed,budget_exhausted} | `pytest tests/test_planner_smoke.py` | 必过 | ☐ |
| C4 | 规则 pipeline e2e(stub) | steps/ >=4 子目录;report.md 存在;metrics 含 sim_passed | `pytest tests/test_e2e_stub.py` | 必过 | ☐ |
| C5 | LLM planner tool-use 回环 | 回灌 Message 含 tool_call_id 与上游一致;幻觉 tool 名返回 eda.tool_not_found | `pytest tests/test_llm_planner.py`(4 用例) | 必过 | ☐ |
| C6 | 预算耗尽控制 | run_budget_s=1s 时 status="budget_exhausted",步数 <= max_iter+1 | `pytest tests/test_planner_limits.py::test_budget` | 必过 | ☐ |
| C7 | 最大迭代控制 | planner_max_iterations=2 时 iteration 到 2 即转 REPORTING | `pytest tests/test_planner_limits.py::test_max_iter` | 必过 | ☐ |
| C8 | 致命错早停 | A 返回 severity=fatal → state.fatal=True,后续不执行新 Action | `pytest tests/test_planner_limits.py::test_fatal` | 必过 | ☐ |
| C9 | 退出码一致性 | budget_exhausted→2;failed→1;report 不存在→64 | `pytest tests/test_cli_exit_codes.py`(3 用例) | 必过 | ☐ |
| C10 | 工件传递 | opensta args.netlist == yosys_synth.artifacts[0] 的 artifact_ref | `pytest tests/test_action_exec.py::test_artifact_pass` | 必过 | ☐ |
| C11 | Tool 注册可扩展 | 注册 stub_ping,C 能发现并调用 | `pytest tests/test_registry_extensibility.py` | 必过 | ☐ |
| C12 | planner_mode 默认 llm | settings [planner] mode == "llm";演示主路径用 llm run | 人工 + 配置检查 | 必过 | ☐ |
| C13 | 独立验证步存在 | B all_pass 的 run,父 RunRecord.steps 含一个非 self_heal 的 iverilog_sim 步且 passed=True | `pytest tests/test_independent_verify.py -m needs_eda` | 必过 | ☐ |
| C14 | PlannerState.history 字段 | _build_messages 能遍历 state.history(非 AttributeError) | `pytest tests/test_llm_planner.py::test_build_messages` | 必过 | ☐ |
| C15 | iteration 单一计数出口 | _reflect 不碰 state.iteration,统一在 _execute_action +1;max_iter 用例无双重计数 | `pytest tests/test_planner_limits.py::test_iteration_no_double` | 必过 | ☐ |

---

## 5. 系统集成验收项

| # | 指标 | 量化门槛 | 测试方法 | 权重 | 勾选 |
|---|---|---|---|---|---|
| I1 | CLI 三子命令 | self-heal / diagnose / report;--help 列出 | `eda --help` | 必过 | ☐ |
| I2 | CLI 退出码 | 0/1/2/64 四态映射正确 | §3 C9 | 必过 | ☐ |
| I3 | e2e 可执行性(必过A) | status ∈ 三态;B 触发一次;experiment_manifest.json 14 字段齐全 | `pytest tests/test_e2e_pipeline.py -m needs_eda` | 必过 | ☐ |
| I4 | 修复效力(必过B) | >= 1 个 inject bug RunReport.status=ok 且 _convergence_cause=all_pass | experiment_summary.json.by_fault_type 统计 | 必过 | ☐ |
| I5 | 独立验证步(必过C) | B all_pass 的 run 有独立 iverilog_sim 步 passed=True | §3 C13 | 必过 | ☐ |
| I6 | baseline run 定义 | 每个 inject bug 有 baseline_run_id + baseline_pass_rate | experiment_manifest.json 字段非空 | 必过 | ☐ |
| I7 | experiment_summary 聚合 | min_group_pass_rate >= 0.50 且 bitwidth.passed >= 1 | `scripts/summarize_eval.py` 产出 + 字段断言 | 必过 | ☐ |
| I8 | fault_manifest 完整 | >= 8 条;bitwidth/comb_logic/timing_reset/syntax 各 >= 2;每条 healable+ground_truth_patch | `python -c "import json; ...字段断言"` | 必过 | ☐ |
| I9 | inject bug 目录统一 | 全部在 data/examples/(无 data/rtl_samples/) | `ls data/examples/` + grep 源码无 rtl_samples | 必过 | ☐ |
| I10 | 三处接口签名一致 | CLI/SDK/MCP 入参字段名与 RunRequest dataclass 完全一致 | 非技术成员字段级互查(Day8) | 必过 | ☐ |
| I11 | namespace 登记 | diagnose(A)+ heal(B) 在 CONTRACTS §2.6 表;C 按 namespace 聚类 | 文档互查 + `pytest tests/test_contracts.py::test_namespace_registry` | 必过 | ☐ |
| I12 | contract_version 一致 | A/B/C/contracts 全部 from eda_agent.contracts import CONTRACT_VERSION,无裸字符串 | grep + ast 检查 | 必过 | ☐ |
| I13 | LLM 计数准确 | RunRecord.llm_calls 与实际 provider 调用次数一致(±0) | CountingProvider 单测 + e2e 后比对 | 必过 | ☐ |
| I14 | MCP(加分) | Claude Code/Cursor 能调 eda_agent.self_heal 拿 RunReport dict | 人工接 server | 加分 | ☐ |
| I15 | OpenAICompat(加分) | Qwen/DeepSeek 切换后 C 代码零改能跑 | provider 切换对比实验 | 加分 | ☐ |

---

## 6. 评审无工具时的替代证据(needs_eda skip 场景)

评审机若未装 yosys/iverilog/opensta,needs_eda 测试会 skip。此时团队提交以下替代证据(准备期 Day9 预跑):

| # | 替代证据 | 要求 | 勾选 |
|---|---|---|---|
| E1 | runs/eval_snapshot/ 预跑快照 | >= 4 个真实 run 目录(各含 1 个 inject bug,详见 runs/eval_snapshot/manifests/) | ☐ |
| E2 | experiment_manifest.json 截图 | 每个 run 的 14 字段齐全(run_id/baseline_run_id/design_id/fault_type/baseline_pass_rate/self_heal_pass_rate/self_heal_convergence/self_heal_best_iter/planner_iterations/llm_calls/tokens_total/wall_time_s/candidates_at_best_score/contract_version) | ☐ |
| E3 | experiment_summary.json | min_group_pass_rate >= 0.50 且 bitwidth.passed >= 1 | ☐ |
| E4 | trajectory 截图 | 至少 1 个 all_pass run 的完整 trajectory(patch diff + sim_result + 独立验证步) | ☐ |
| E5 | 现场重跑(若评审允许) | 团队自带环境现场重跑 1-2 个 inject bug 复核 | ☐ |

---

## 7. 验收流程(Claude 如何验收)

### 7.1 验收前置(团队 Day9 提交前自检)

1. 团队按 §1-§5 逐项勾选,所有"必过"项必须 ☑。
2. 非技术成员执行三份组件文档与 CONTRACTS.md 的字段级互查(Day8 任务),记 checklist。
3. 团队 Day9 预跑 eval_snapshot,填 §6 替代证据。

### 7.2 Claude 验收步骤(评审现场或远程)

Claude 验收按以下顺序执行(每步给 PASS/FAIL,FAIL 则定位 + 反馈修复建议):

**Step 1:环境与依赖检查**
- 读 settings.toml + pyproject.toml,核版本(yosys>=0.30/iverilog>=12/opensta>=2.3/python>=3.10/anthropic>=0.40/jsonschema>=4)。
- `pip install -e .` 成功;`eda --help` 列三子命令(I1)。
- 若 WSL2 工具不可用 → 跳到 Step 6(替代证据)。

**Step 2:契约与基座单测(无 needs_eda)**
- `pytest tests/test_contracts.py tests/test_registry.py -v`(I11/I12 + dataclass 字段完整性 + artifact_ref 工厂)。
- `python scripts/check_no_subprocess.py`(C1/C2)。

**Step 3:组件单测(无 needs_eda,全 mock)**
- `pytest tests/test_diagnose_skill.py tests/test_self_heal_skill.py -v`(A5-A14 + B3/B6-B8/B10 + C5/C14/C15 的 mock 部分)。

**Step 4:真跑 EDA(needs_eda)**
- `pytest tests/test_yosys_tool.py tests/test_iverilog_tool.py tests/test_opensta_tool.py -v -m needs_eda`(S1 工具部分)。
- `pytest tests/test_e2e_pipeline.py -v -m needs_eda`(I3 必过A + I4 必过B)+ `pytest tests/test_independent_verify.py -v -m needs_eda`(I5/C13 必过C 独立验证步)。
- `pytest tests/test_self_heal_skill.py::test_pass_rate -v -m needs_eda`(B1)+ `scripts/summarize_eval.py`(I7)。

**Step 5:人工/文档验收**
- I8 fault_manifest.json 字段;I9 目录统一;I10 三处接口签名;A11/A12 语料规模与抽检。
- 查 runs/<run_id>/report.md 人类可读,含 root_cause/convergence/独立验证步渲染。

**Step 6:替代证据(评审无工具时)**
- 读 runs/eval_snapshot/ + experiment_manifest.json + experiment_summary.json,核 §6 E1-E5。

**Step 7:加分项(有余力)**
- I14 MCP;I15 OpenAICompat。

### 7.3 验收结论

- 所有"必过"项 ☑ → 完赛奖达成。
- 任一"必过"FAIL → Claude 给出 FAIL 项编号 + 修复建议(回指对应组件文档 §),团队修复后重跑该 Step。
- "加分"项不影响完赛奖判定,但影响评委对"为基座贡献关键能力"的印象分。

### 7.4 Claude 验收输出格式

    验收结论:PASS / FAIL / PARTIAL(部分必过)
    必过项:☑/☐ 列表(编号 + 一句话证据)
    失败项(若有):编号 + 失败原因 + 修复建议(回指文档 §)
    加分项:☑/☐
    替代证据(若适用):E1-E5 ☑/☐
    建议下一步:具体动作(如"修复 B §6 fail_signals 包装后重跑 B8")
