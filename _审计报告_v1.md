# 接口一致性审计报告 v1(修复前基线)

> 独立第三方审计(审计员未参与文档生成),2026-07-03。
> 审计对象:`D:/eda agent system/` 下 CONTRACTS.md(权威)+ 组件A/B/C + ARCHITECTURE.md + 验收标准.md + README.md。
> 目的:在交付新终端实现前,字段级/路径级核对三份组件文档与权威契约是否零冲突。

## 总体判断

**需大修**。2 blocker + 5 major + 3 minor。**不可直接交付新终端实现**。核心问题:组件 C 的 CLI 子命令数与契约全面冲突;A/B/C 的 `build_registry` 工厂调用漏传 `runner`(契约 v1.2 头号修复点)。

---

## 🔴 Blockers(2)

### B1. CLI 子命令数:C 文档 vs 契约/ARCHITECTURE/README/验收标准
- CONTRACTS / ARCHITECTURE / README / 验收标准:**3 个**(`eda self-heal` / `eda diagnose` / `eda report`)。
- 组件 C:反复说**2 个**(self-heal + report),明文"diagnose 不单独暴露"(§1.1、§5.2、Step C7、§10.4)。
- 后果:照 C 实现 → `eda diagnose` 不存在 → 验收 I1 挂 + 演示 A 诊断器能力的入口没了。
- 修复:C 改为 3 子命令;§5.2 补 `eda diagnose` 块(照搬契约 §2.0 / ARCHITECTURE §5.1);删"diagnose 不单独暴露";Step C7/§10.4 改"三子命令"。

### B2. `build_registry` 漏传 `runner` + `tools_bootstrap` 命名
- 契约 §3:`build_registry(provider, runner, settings)` 三参数;import 路径 `eda_agent.tools.bootstrap`(文件 `tools/bootstrap.py`)。v1.2 把"Skill 构造统一注入 runner"列为头号修复。
- 组件 A(§7、§9 步骤8):`build_registry(provider=..., settings=...)` 漏 `runner=`;`DiagnoseSkill(kb, provider, settings)` 漏 `runner`。
- 组件 B(§9 步骤B7):`SelfHealSkill(registry=, llm=, max_iterations=)` 漏 `runner=` 与 `settings=`,与 B §5.1 自身签名矛盾。
- 三文档均用旧模块名 `tools_bootstrap`。
- 后果:照抄 → Skill 无 runner 句柄 → 子步无法 append_step → RunRecord.steps 缺 B 内部迭代轨迹 → 验收 B4 挂。
- 修复:A/B/C 补 `runner=` 参数;统一 `tools.bootstrap`。

---

## 🟡 Majors(5)

### M1. ErrorKB.lookup 签名 A 内部矛盾
- A §3.1 注释 `lookup(tool, error_code) -> ErrorPattern|None` vs §4.2 定义 `lookup(self, tool, log_text) -> list[tuple[ErrorPattern, Match]]`。统一为 §4.2 版本。

### M2. SkillResult.best_iter 契约内部矛盾
- 契约 §2.3 dataclass 默认 `best_iter=-1`(无任何轮通过),同节 A 恒定语义写 `best_iter=0`。0 会让 C 误判 A 找到最佳轮。
- 修复:契约 §2.3 A 恒定值改 `-1`;A §5.1 / §10.3 D 项同步 `_best_iter==-1`。

### M3. `_artifact_ref` reserved 字段未登记
- 契约 §2.3 reserved 表只举 `_remaining_budget_s`;C 用的 `_artifact_ref`(由 C._resolve_args 处理工件注入)未登记。
- 修复:契约 §2.3 增列 `_artifact_ref`,注明 reserved 统一由调用方处理,as_tool 仅负责 `_remaining_budget_s`。

### M4. RunReport.metrics 字段集 C 缺两字段
- 契约 §2.4 v1.2 锁定 13 字段;C §5.5 只给 11,缺 `self_heal_pass_rate` + `baseline_pass_rate`;还引用过时 v1.1 minor#13;§12 #7 与 v1.2 已裁决矛盾。
- 修复:C §5.5 补两字段;删旧 minor 引用;§12 #7 标 [v1.2 已裁决]。

### M5. tests/ 目录树缺文件
- CONTRACTS §4 / ARCHITECTURE §4 tests/ 段缺 `test_registry_extensibility.py`、`test_diagnose_rule.py`、`test_independent_verify.py`(组件/验收引用但目录树未列)。
- 后果:新终端按目录树建会缺文件,跑验收 FileNotFoundError。
- 修复:两个目录树补齐(`test_namespace_registry` 注明并入 `test_contracts`)。

---

## ⚪ Minors(3,集训期清)

- m1. DiagnosisReport 字段 `errors` vs parsed 输出 `root_causes` 命名不一(A §4.3)。
- m2. iverilog `passed=None` 判定:B 用 `is not False`、C 用 `is True`,None 语义不一。
- m3. `check_no_subprocess.py`:契约 §4 scripts 未列;C §10.1 一表内 grep 与 ast.walk 残留混用。

---

## 维度小结

| 维度 | 结论 |
|---|---|
| 1 dataclass/类型 | 基本一致(import 复用);问题:M1(lookup)、M2(best_iter)、m1 |
| 2 函数签名 | run_pipeline/execute/run/as_tool/artifact_ref/make_provider 一致;**两 blocker**(build_registry、CLI 子命令) |
| 3 as_tool 语义 | 完全一致(budget 塌缩/拆包/_skill_status);边界问题 M3 |
| 4 错误码 namespace | 完全一致 |
| 5 parsed._schema/CONTRACT_VERSION | 完全一致(无裸 "0.1.0") |
| 6 路径/目录树 | 大体一致;问题:M5(tests 缺)、B2(tools_bootstrap)、m3 |

## 结论与修复顺序

不可直接交付。修完 2 blocker(约 1-2h 纯文档改)+ 5 major(2-3h)后可交付。minor 集训期清。
顺序:B1/B2 → M2 → M4 → M5 → M1/M3 → minor。
